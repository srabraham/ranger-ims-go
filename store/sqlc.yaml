version: "2"
sql:
  - engine: "mysql"
    queries: "queries.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "queries"
        out: "queries"
        emit_enum_valid_method: true
        emit_all_enum_values: true